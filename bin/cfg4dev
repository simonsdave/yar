if [ -f "$PWD/.801dbe4659a641739cbe94fcf0baab03" ]; then
	source ~/virtualenv/apimgmt/bin/activate
	export PATH=$PATH:"$PWD/bin"
	export PYTHONPATH="$PWD/lib"
else
	echo "Must source this script from root dir of apimgmt project"
fi
